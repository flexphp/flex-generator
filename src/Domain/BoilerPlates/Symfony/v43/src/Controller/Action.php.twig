{% set methods = methods|default('GET')|upper %}
    /**
     * @Route("{{ route|lower }}"}, methods={"{{ methods }}"}, name="{{ entity|lower }}.{{ action|lower }}")
{% if methods == 'GET' %}
     * @Cache(smaxage="{{ cache|default('10') }}")
{% endif %}
     */
{% if methods in ['PUT'] %}
    public function {{ action_camel }}(Request $request, $id): Response
{% elseif (methods == 'GET' and action == 'read') or methods == 'DELETE' %}
    public function {{ action_camel }}($id): Response
{% else %}
    public function {{ action_camel }}(Request $request): Response
{% endif %}
    {
{% if request_message %}
        {{ request_message|trim(side='left')|raw }}
{% endif %}
{% if use_case %}

{% endif %}
{% if use_case %}
        {{ use_case|trim(side='left')|raw }}
{% endif %}
{% if response_message %}

{% endif %}
{% if response_message %}
        {{ response_message|trim(side='left')|raw }}
{% endif %}
    }
