{% if action|lower in ['read', 'delete'] %}
        $request = new {{ action }}{{ entity }}Request(['id' => $id]);
{% else %}
        $request = new {{ action }}{{ entity }}Request($request->request->all());
{% endif %}