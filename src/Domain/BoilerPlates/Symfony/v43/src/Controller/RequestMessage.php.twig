{% if action in ['read', 'delete'] %}
        $request = new {{ action_name }}{{ entity }}Request(['id' => $id]);
{% else %}
        $request = new {{ action_name }}{{ entity }}Request($request->request->all());
{% endif %}