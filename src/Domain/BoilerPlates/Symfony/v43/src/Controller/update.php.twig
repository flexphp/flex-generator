    /**
     * @Route("/edit/{id}", methods={"GET"}, name="{{ entity_dash }}.edit")
     * @Cache(smaxage="10")
     * @Security("is_granted('ROLE_ADMIN') or is_granted('ROLE_USER_{{ entity|upper }}_{{ action_camel|upper }}')", statusCode=401)
     */
    public function edit(Connection $conn, $id): Response
    {
        $request = new Read{{ entity }}Request($id);

        $useCase = new Read{{ entity }}UseCase(new {{ entity }}Repository(new MySQL{{ entity }}Gateway($conn)));

        $response = $useCase->execute($request);

        $form = $this->createForm({{ entity }}FormType::class, $response->{{ name }});

        return $this->render('{{ name }}/edit.html.twig', [
            'register' => $response->{{ name }},
            'form' => $form->createView(),
        ]);
    }

    /**
     * @Route("{{ route }}", methods={"{{ methods }}"}, name="{{ route_name }}")
     * @Security("is_granted('ROLE_ADMIN') or is_granted('ROLE_USER_{{ entity|upper }}_{{ action_camel|upper }}')", statusCode=401)
     */
    public function {{ action_camel }}(Request $request, Connection $conn): Response
    {
{% if request_message %}
        {{ request_message|trim|raw }}
{% endif %}
{% if request_message and use_case %}

{% endif %}
{% if use_case %}
        {{ use_case|trim|raw }}
{% endif %}
{% if use_case and response_message %}

{% endif %}
{% if response_message %}
        {{ response_message|trim|raw }}
{% endif %}
    }
