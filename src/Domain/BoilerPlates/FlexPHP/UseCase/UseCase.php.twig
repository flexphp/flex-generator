<?php declare(strict_types=1);

namespace Domain\{{ entity }}\UseCase;

use Domain\{{ entity }}\Message\{{ action_name }}{{ entity }}Request;
use Domain\{{ entity }}\Message\{{ action_name }}{{ entity }}Response;
use FlexPHP\UseCases\UseCase;

final class {{ action_name }}{{ entity }}UseCase extends UseCase
{
{% for name, property in properties %}
    private ${{ name }};
{% if loop.last %}

{% endif %}
{% endfor %}
    /**
     * @param {{ action_name }}{{ entity }}Request $request
     *
     * @return {{ action_name }}{{ entity }}Response
     */
    public function execute($request)
    {
{% for name, property in properties %}
        $this->{{ name }} = $request->{{ name }};
{% if loop.last %}

{% endif %}
{% endfor %}
        return {{ action_name }}{{ entity }}Response();
    }
}
