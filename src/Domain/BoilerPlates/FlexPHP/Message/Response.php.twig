<?php declare(strict_types=1);

namespace Domain\{{ entity }}\Response;

{% if action in ['Create', 'Read', 'Login'] %}
use Domain\{{ entity }}\{{ entity }};
{% endif %}
use FlexPHP\Messages\ResponseInterface;

final class {{ action }}{{ entity }}Response implements ResponseInterface
{
{% if action == 'Index' %}
    public ${{ items }};

    public function __construct(array ${{ items }})
    {
        $this->{{ items }} = ${{ items }};
    }
{% elseif action in ['Update', 'Delete'] %}
    public $code;
    public $status;
    public $message;

    public function __construct(int $code, string $status, string $message)
    {
        $this->code = $code;
        $this->status = $status;
        $this->message = $message;
    }
{% elseif action in ['Create', 'Read', 'Login'] %}
    public ${{ item }};

    public function __construct({{ entity }} ${{ item }})
    {
        $this->{{ item }} = ${{ item }};
    }
{% else %}
    public function __construct(array $data = [])
    {
    }
{% endif %}
}
