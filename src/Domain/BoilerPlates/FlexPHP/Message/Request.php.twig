{% if action == 'read' %}
        $requestMessage = new {{ action_name }}{{ entity }}Request(['id' => $id]);
{% else %}
        $requestMessage = new {{ action_name }}{{ entity }}Request($request->request->all());
{% endif %}