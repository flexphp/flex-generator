<?php declare(strict_types=1);
{{ header|raw }}
namespace Domain\{{ pkEntity }}\Request;

use FlexPHP\Messages\RequestInterface;

final class Find{{ pkEntity }}{{ fkEntity }}Request implements RequestInterface
{
    public $term;

    public $_page;

    public $_limit;

    public function __construct(array $data)
    {
        $this->term = $data['term'] ?? '';
        $this->_page = $data['page'] ?? 1;
        $this->_limit = $data['limit'] ?? 20;
    }
}
